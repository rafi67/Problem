#include <bits/stdc++.h>

using namespace std;

int main() {

    double num;
    cin >> num;

    cout << showpos << fixed << setprecision(4) << scientific << uppercase 
    << num << "\n";

    return 0;

}